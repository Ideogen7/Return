# Normalize all text files to LF (prevents CRLF drift on Windows/WSL)
* text=auto eol=lf

# Force LF line endings for shell scripts (cross-platform hook compatibility)
*.sh text eol=lf
frontend/.husky/* text eol=lf
