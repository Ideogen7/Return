# Activer le mock Prism pour le développement
USE_MOCK=true

# (Optionnel) Forcer l'URL du backend, en restant en local.
# Décommenter pour brancher le front local sur le backend Fly.io déployé
# (utile pour tester à plusieurs sur le même back + la même BDD) :
# EXPO_PUBLIC_API_URL=https://return-api.fly.dev/v1
