# =============================================================================
# Return Frontend — Dockerfile Development (hot reload)
# =============================================================================

FROM node:22-slim

WORKDIR /app

COPY package.json package-lock.json* ./
RUN npm ci

COPY . .

EXPOSE 8081

CMD ["npx", "expo", "start", "--web", "--host", "lan"]
